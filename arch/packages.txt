# arch/packages.txt
# Arch Linux packages equivalent to the Homebrew/Ubuntu packages in this repo.
# Install with: grep -vE '^\s*(#|$)' arch/packages.txt | xargs sudo pacman -S --needed
#
# Only official repo packages are listed here. AUR-only tools are intentionally
# omitted so bootstrap.sh works on a fresh pacman-only machine.

# --- Core utilities ---
base-devel
curl
git
unzip

# --- Shell ---
zsh

# --- Python (needed by Mason/Neovim for Python-based tools) ---
python
python-pip

# --- CLI tools ---
aws-cli
bat
bat-extras
bottom
ctags
docker-compose
fd
fzf
go
helm
jq
k9s
kubectx
lazydocker
lazygit
neovim
nushell
podman
ripgrep
starship
stow
thefuck
tig
tmux
tree
tree-sitter-cli
xh
yarn
yazi
yq
zellij
zoxide

# --- Installed by bootstrap scripts instead of pacman ---
# bun: scripts/bun.sh
# herdr: scripts/herdr.sh
# nvm + Node.js 22: bootstrap.sh curl installer
# sdkman/kotlin/gradle: bootstrap.sh
#
# --- Usados pelos aliases do .zshrc (top, ping, kubectl, tf, ytd*) ---
htop
kubectl
prettyping
terraform
yt-dlp

# --- AUR: ver arch/aur.txt (bootstrap.sh instala com yay quando existir) ---
# kubecolor, wtfutil
#
# --- Manual ---
# opencode: no Omarchy vem via mise (~/.config/mise/config.toml)
# tfswitch
